/*
 * Generated by gdbus-codegen 2.46.2. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "multipath_dbus_generated.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface com.redhat.multipath.Manager
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:multipathManager
 * @title: multipathManager
 * @short_description: Generated C code for the com.redhat.multipath.Manager D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-redhat-multipath-Manager.top_of_page">com.redhat.multipath.Manager</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.redhat.multipath.Manager ---- */

static const _ExtendedGDBusArgInfo _multipath_manager_method_info_reload_OUT_ARG_response =
{
  {
    -1,
    (gchar *) "response",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _multipath_manager_method_info_reload_OUT_ARG_pointers[] =
{
  &_multipath_manager_method_info_reload_OUT_ARG_response,
  NULL
};

static const _ExtendedGDBusMethodInfo _multipath_manager_method_info_reload =
{
  {
    -1,
    (gchar *) "reload",
    NULL,
    (GDBusArgInfo **) &_multipath_manager_method_info_reload_OUT_ARG_pointers,
    NULL
  },
  "handle-reload",
  FALSE
};

static const _ExtendedGDBusArgInfo _multipath_manager_method_info_flush_OUT_ARG_response =
{
  {
    -1,
    (gchar *) "response",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _multipath_manager_method_info_flush_OUT_ARG_pointers[] =
{
  &_multipath_manager_method_info_flush_OUT_ARG_response,
  NULL
};

static const _ExtendedGDBusMethodInfo _multipath_manager_method_info_flush =
{
  {
    -1,
    (gchar *) "flush",
    NULL,
    (GDBusArgInfo **) &_multipath_manager_method_info_flush_OUT_ARG_pointers,
    NULL
  },
  "handle-flush",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _multipath_manager_method_info_pointers[] =
{
  &_multipath_manager_method_info_reload,
  &_multipath_manager_method_info_flush,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _multipath_manager_interface_info =
{
  {
    -1,
    (gchar *) "com.redhat.multipath.Manager",
    (GDBusMethodInfo **) &_multipath_manager_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "manager",
};


/**
 * multipath_manager_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-redhat-multipath-Manager.top_of_page">com.redhat.multipath.Manager</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
multipath_manager_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_multipath_manager_interface_info.parent_struct;
}

/**
 * multipath_manager_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #multipathManager interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
multipath_manager_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * multipathManager:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-Manager.top_of_page">com.redhat.multipath.Manager</link>.
 */

/**
 * multipathManagerIface:
 * @parent_iface: The parent interface.
 * @handle_flush: Handler for the #multipathManager::handle-flush signal.
 * @handle_reload: Handler for the #multipathManager::handle-reload signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-Manager.top_of_page">com.redhat.multipath.Manager</link>.
 */

typedef multipathManagerIface multipathManagerInterface;
G_DEFINE_INTERFACE (multipathManager, multipath_manager, G_TYPE_OBJECT);

static void
multipath_manager_default_init (multipathManagerIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * multipathManager::handle-reload:
   * @object: A #multipathManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-redhat-multipath-Manager.reload">reload()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call multipath_manager_complete_reload() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-reload",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (multipathManagerIface, handle_reload),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * multipathManager::handle-flush:
   * @object: A #multipathManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-redhat-multipath-Manager.flush">flush()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call multipath_manager_complete_flush() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-flush",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (multipathManagerIface, handle_flush),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

}

/**
 * multipath_manager_call_reload:
 * @proxy: A #multipathManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-redhat-multipath-Manager.reload">reload()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call multipath_manager_call_reload_finish() to get the result of the operation.
 *
 * See multipath_manager_call_reload_sync() for the synchronous, blocking version of this method.
 */
void
multipath_manager_call_reload (
    multipathManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "reload",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * multipath_manager_call_reload_finish:
 * @proxy: A #multipathManagerProxy.
 * @out_response: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to multipath_manager_call_reload().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with multipath_manager_call_reload().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
multipath_manager_call_reload_finish (
    multipathManager *proxy,
    gint *out_response,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(i)",
                 out_response);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * multipath_manager_call_reload_sync:
 * @proxy: A #multipathManagerProxy.
 * @out_response: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-redhat-multipath-Manager.reload">reload()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See multipath_manager_call_reload() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
multipath_manager_call_reload_sync (
    multipathManager *proxy,
    gint *out_response,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "reload",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(i)",
                 out_response);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * multipath_manager_call_flush:
 * @proxy: A #multipathManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-redhat-multipath-Manager.flush">flush()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call multipath_manager_call_flush_finish() to get the result of the operation.
 *
 * See multipath_manager_call_flush_sync() for the synchronous, blocking version of this method.
 */
void
multipath_manager_call_flush (
    multipathManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "flush",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * multipath_manager_call_flush_finish:
 * @proxy: A #multipathManagerProxy.
 * @out_response: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to multipath_manager_call_flush().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with multipath_manager_call_flush().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
multipath_manager_call_flush_finish (
    multipathManager *proxy,
    gint *out_response,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(i)",
                 out_response);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * multipath_manager_call_flush_sync:
 * @proxy: A #multipathManagerProxy.
 * @out_response: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-redhat-multipath-Manager.flush">flush()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See multipath_manager_call_flush() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
multipath_manager_call_flush_sync (
    multipathManager *proxy,
    gint *out_response,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "flush",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(i)",
                 out_response);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * multipath_manager_complete_reload:
 * @object: A #multipathManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @response: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-redhat-multipath-Manager.reload">reload()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
multipath_manager_complete_reload (
    multipathManager *object,
    GDBusMethodInvocation *invocation,
    gint response)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(i)",
                   response));
}

/**
 * multipath_manager_complete_flush:
 * @object: A #multipathManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @response: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-redhat-multipath-Manager.flush">flush()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
multipath_manager_complete_flush (
    multipathManager *object,
    GDBusMethodInvocation *invocation,
    gint response)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(i)",
                   response));
}

/* ------------------------------------------------------------------------ */

/**
 * multipathManagerProxy:
 *
 * The #multipathManagerProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * multipathManagerProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #multipathManagerProxy.
 */

struct _multipathManagerProxyPrivate
{
  GData *qdata;
};

static void multipath_manager_proxy_iface_init (multipathManagerIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (multipathManagerProxy, multipath_manager_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (multipathManagerProxy)
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_MANAGER, multipath_manager_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (multipathManagerProxy, multipath_manager_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_MANAGER, multipath_manager_proxy_iface_init));

#endif
static void
multipath_manager_proxy_finalize (GObject *object)
{
  multipathManagerProxy *proxy = MULTIPATH_MANAGER_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (multipath_manager_proxy_parent_class)->finalize (object);
}

static void
multipath_manager_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
multipath_manager_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
multipath_manager_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_multipath_manager_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], MULTIPATH_TYPE_MANAGER);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, MULTIPATH_TYPE_MANAGER);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
multipath_manager_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  multipathManagerProxy *proxy = MULTIPATH_MANAGER_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_multipath_manager_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_multipath_manager_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
multipath_manager_proxy_init (multipathManagerProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = multipath_manager_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, MULTIPATH_TYPE_MANAGER_PROXY, multipathManagerProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), multipath_manager_interface_info ());
}

static void
multipath_manager_proxy_class_init (multipathManagerProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = multipath_manager_proxy_finalize;
  gobject_class->get_property = multipath_manager_proxy_get_property;
  gobject_class->set_property = multipath_manager_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = multipath_manager_proxy_g_signal;
  proxy_class->g_properties_changed = multipath_manager_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (multipathManagerProxyPrivate));
#endif
}

static void
multipath_manager_proxy_iface_init (multipathManagerIface *iface)
{
}

/**
 * multipath_manager_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-Manager.top_of_page">com.redhat.multipath.Manager</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call multipath_manager_proxy_new_finish() to get the result of the operation.
 *
 * See multipath_manager_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
multipath_manager_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (MULTIPATH_TYPE_MANAGER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.redhat.multipath.Manager", NULL);
}

/**
 * multipath_manager_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to multipath_manager_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with multipath_manager_proxy_new().
 *
 * Returns: (transfer full) (type multipathManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
multipathManager *
multipath_manager_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return MULTIPATH_MANAGER (ret);
  else
    return NULL;
}

/**
 * multipath_manager_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-Manager.top_of_page">com.redhat.multipath.Manager</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See multipath_manager_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type multipathManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
multipathManager *
multipath_manager_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (MULTIPATH_TYPE_MANAGER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.redhat.multipath.Manager", NULL);
  if (ret != NULL)
    return MULTIPATH_MANAGER (ret);
  else
    return NULL;
}


/**
 * multipath_manager_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like multipath_manager_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call multipath_manager_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See multipath_manager_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
multipath_manager_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (MULTIPATH_TYPE_MANAGER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.redhat.multipath.Manager", NULL);
}

/**
 * multipath_manager_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to multipath_manager_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with multipath_manager_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type multipathManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
multipathManager *
multipath_manager_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return MULTIPATH_MANAGER (ret);
  else
    return NULL;
}

/**
 * multipath_manager_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like multipath_manager_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See multipath_manager_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type multipathManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
multipathManager *
multipath_manager_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (MULTIPATH_TYPE_MANAGER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.redhat.multipath.Manager", NULL);
  if (ret != NULL)
    return MULTIPATH_MANAGER (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * multipathManagerSkeleton:
 *
 * The #multipathManagerSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * multipathManagerSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #multipathManagerSkeleton.
 */

struct _multipathManagerSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_multipath_manager_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  multipathManagerSkeleton *skeleton = MULTIPATH_MANAGER_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], MULTIPATH_TYPE_MANAGER);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, MULTIPATH_TYPE_MANAGER);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_multipath_manager_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  multipathManagerSkeleton *skeleton = MULTIPATH_MANAGER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_multipath_manager_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_multipath_manager_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  multipathManagerSkeleton *skeleton = MULTIPATH_MANAGER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_multipath_manager_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _multipath_manager_skeleton_vtable =
{
  _multipath_manager_skeleton_handle_method_call,
  _multipath_manager_skeleton_handle_get_property,
  _multipath_manager_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
multipath_manager_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return multipath_manager_interface_info ();
}

static GDBusInterfaceVTable *
multipath_manager_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_multipath_manager_skeleton_vtable;
}

static GVariant *
multipath_manager_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  multipathManagerSkeleton *skeleton = MULTIPATH_MANAGER_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_multipath_manager_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _multipath_manager_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _multipath_manager_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _multipath_manager_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.redhat.multipath.Manager", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
multipath_manager_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void multipath_manager_skeleton_iface_init (multipathManagerIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (multipathManagerSkeleton, multipath_manager_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (multipathManagerSkeleton)
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_MANAGER, multipath_manager_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (multipathManagerSkeleton, multipath_manager_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_MANAGER, multipath_manager_skeleton_iface_init));

#endif
static void
multipath_manager_skeleton_finalize (GObject *object)
{
  multipathManagerSkeleton *skeleton = MULTIPATH_MANAGER_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (multipath_manager_skeleton_parent_class)->finalize (object);
}

static void
multipath_manager_skeleton_init (multipathManagerSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = multipath_manager_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, MULTIPATH_TYPE_MANAGER_SKELETON, multipathManagerSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
multipath_manager_skeleton_class_init (multipathManagerSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = multipath_manager_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = multipath_manager_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = multipath_manager_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = multipath_manager_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = multipath_manager_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (multipathManagerSkeletonPrivate));
#endif
}

static void
multipath_manager_skeleton_iface_init (multipathManagerIface *iface)
{
}

/**
 * multipath_manager_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-Manager.top_of_page">com.redhat.multipath.Manager</link>.
 *
 * Returns: (transfer full) (type multipathManagerSkeleton): The skeleton object.
 */
multipathManager *
multipath_manager_skeleton_new (void)
{
  return MULTIPATH_MANAGER (g_object_new (MULTIPATH_TYPE_MANAGER_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.redhat.multipath.map
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:multipathMap
 * @title: multipathMap
 * @short_description: Generated C code for the com.redhat.multipath.map D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-redhat-multipath-map.top_of_page">com.redhat.multipath.map</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.redhat.multipath.map ---- */

static const _ExtendedGDBusPropertyInfo _multipath_map_property_info_uuid =
{
  {
    -1,
    (gchar *) "uuid",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "uuid",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_property_info_size =
{
  {
    -1,
    (gchar *) "size",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "size",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_property_info_features =
{
  {
    -1,
    (gchar *) "features",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "features",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_property_info_pgfailback =
{
  {
    -1,
    (gchar *) "pgfailback",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "pgfailback",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_property_info_pg_policy =
{
  {
    -1,
    (gchar *) "pg_policy",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "pg-policy",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_property_info_nextpg =
{
  {
    -1,
    (gchar *) "nextpg",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "nextpg",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_property_info_bestpg =
{
  {
    -1,
    (gchar *) "bestpg",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "bestpg",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_property_info_queuedio =
{
  {
    -1,
    (gchar *) "queuedio",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "queuedio",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_property_info_no_path_retry =
{
  {
    -1,
    (gchar *) "no_path_retry",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "no-path-retry",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_property_info_nr_active =
{
  {
    -1,
    (gchar *) "nr_active",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "nr-active",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_property_info_selector =
{
  {
    -1,
    (gchar *) "selector",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "selector",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_property_info_paths =
{
  {
    -1,
    (gchar *) "paths",
    (gchar *) "ao",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "paths",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_property_info_pathgroups =
{
  {
    -1,
    (gchar *) "pathgroups",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "pathgroups",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _multipath_map_property_info_pointers[] =
{
  &_multipath_map_property_info_uuid,
  &_multipath_map_property_info_size,
  &_multipath_map_property_info_features,
  &_multipath_map_property_info_pgfailback,
  &_multipath_map_property_info_pg_policy,
  &_multipath_map_property_info_nextpg,
  &_multipath_map_property_info_bestpg,
  &_multipath_map_property_info_queuedio,
  &_multipath_map_property_info_no_path_retry,
  &_multipath_map_property_info_nr_active,
  &_multipath_map_property_info_selector,
  &_multipath_map_property_info_paths,
  &_multipath_map_property_info_pathgroups,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _multipath_map_interface_info =
{
  {
    -1,
    (gchar *) "com.redhat.multipath.map",
    NULL,
    NULL,
    (GDBusPropertyInfo **) &_multipath_map_property_info_pointers,
    NULL
  },
  "map",
};


/**
 * multipath_map_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-redhat-multipath-map.top_of_page">com.redhat.multipath.map</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
multipath_map_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_multipath_map_interface_info.parent_struct;
}

/**
 * multipath_map_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #multipathMap interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
multipath_map_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "uuid");
  g_object_class_override_property (klass, property_id_begin++, "size");
  g_object_class_override_property (klass, property_id_begin++, "features");
  g_object_class_override_property (klass, property_id_begin++, "pgfailback");
  g_object_class_override_property (klass, property_id_begin++, "pg-policy");
  g_object_class_override_property (klass, property_id_begin++, "nextpg");
  g_object_class_override_property (klass, property_id_begin++, "bestpg");
  g_object_class_override_property (klass, property_id_begin++, "queuedio");
  g_object_class_override_property (klass, property_id_begin++, "no-path-retry");
  g_object_class_override_property (klass, property_id_begin++, "nr-active");
  g_object_class_override_property (klass, property_id_begin++, "selector");
  g_object_class_override_property (klass, property_id_begin++, "paths");
  g_object_class_override_property (klass, property_id_begin++, "pathgroups");
  return property_id_begin - 1;
}



/**
 * multipathMap:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map.top_of_page">com.redhat.multipath.map</link>.
 */

/**
 * multipathMapIface:
 * @parent_iface: The parent interface.
 * @get_bestpg: Getter for the #multipathMap:bestpg property.
 * @get_features: Getter for the #multipathMap:features property.
 * @get_nextpg: Getter for the #multipathMap:nextpg property.
 * @get_no_path_retry: Getter for the #multipathMap:no-path-retry property.
 * @get_nr_active: Getter for the #multipathMap:nr-active property.
 * @get_pathgroups: Getter for the #multipathMap:pathgroups property.
 * @get_paths: Getter for the #multipathMap:paths property.
 * @get_pg_policy: Getter for the #multipathMap:pg-policy property.
 * @get_pgfailback: Getter for the #multipathMap:pgfailback property.
 * @get_queuedio: Getter for the #multipathMap:queuedio property.
 * @get_selector: Getter for the #multipathMap:selector property.
 * @get_size: Getter for the #multipathMap:size property.
 * @get_uuid: Getter for the #multipathMap:uuid property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map.top_of_page">com.redhat.multipath.map</link>.
 */

typedef multipathMapIface multipathMapInterface;
G_DEFINE_INTERFACE (multipathMap, multipath_map, G_TYPE_OBJECT);

static void
multipath_map_default_init (multipathMapIface *iface)
{
  /* GObject properties for D-Bus properties: */
  /**
   * multipathMap:uuid:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map.uuid">"uuid"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("uuid", "uuid", "uuid", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMap:size:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map.size">"size"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("size", "size", "size", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMap:features:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map.features">"features"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("features", "features", "features", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMap:pgfailback:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map.pgfailback">"pgfailback"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("pgfailback", "pgfailback", "pgfailback", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMap:pg-policy:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map.pg_policy">"pg_policy"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("pg-policy", "pg_policy", "pg_policy", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMap:nextpg:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map.nextpg">"nextpg"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("nextpg", "nextpg", "nextpg", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMap:bestpg:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map.bestpg">"bestpg"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("bestpg", "bestpg", "bestpg", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMap:queuedio:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map.queuedio">"queuedio"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("queuedio", "queuedio", "queuedio", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMap:no-path-retry:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map.no_path_retry">"no_path_retry"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("no-path-retry", "no_path_retry", "no_path_retry", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMap:nr-active:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map.nr_active">"nr_active"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("nr-active", "nr_active", "nr_active", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMap:selector:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map.selector">"selector"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("selector", "selector", "selector", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMap:paths:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map.paths">"paths"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("paths", "paths", "paths", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMap:pathgroups:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map.pathgroups">"pathgroups"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("pathgroups", "pathgroups", "pathgroups", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * multipath_map_get_uuid: (skip)
 * @object: A #multipathMap.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map.uuid">"uuid"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use multipath_map_dup_uuid() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
multipath_map_get_uuid (multipathMap *object)
{
  return MULTIPATH_MAP_GET_IFACE (object)->get_uuid (object);
}

/**
 * multipath_map_dup_uuid: (skip)
 * @object: A #multipathMap.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-redhat-multipath-map.uuid">"uuid"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
multipath_map_dup_uuid (multipathMap *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "uuid", &value, NULL);
  return value;
}

/**
 * multipath_map_set_uuid: (skip)
 * @object: A #multipathMap.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map.uuid">"uuid"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_set_uuid (multipathMap *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "uuid", value, NULL);
}

/**
 * multipath_map_get_size: (skip)
 * @object: A #multipathMap.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map.size">"size"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
multipath_map_get_size (multipathMap *object)
{
  return MULTIPATH_MAP_GET_IFACE (object)->get_size (object);
}

/**
 * multipath_map_set_size: (skip)
 * @object: A #multipathMap.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map.size">"size"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_set_size (multipathMap *object, guint value)
{
  g_object_set (G_OBJECT (object), "size", value, NULL);
}

/**
 * multipath_map_get_features: (skip)
 * @object: A #multipathMap.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map.features">"features"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use multipath_map_dup_features() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
multipath_map_get_features (multipathMap *object)
{
  return MULTIPATH_MAP_GET_IFACE (object)->get_features (object);
}

/**
 * multipath_map_dup_features: (skip)
 * @object: A #multipathMap.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-redhat-multipath-map.features">"features"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
multipath_map_dup_features (multipathMap *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "features", &value, NULL);
  return value;
}

/**
 * multipath_map_set_features: (skip)
 * @object: A #multipathMap.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map.features">"features"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_set_features (multipathMap *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "features", value, NULL);
}

/**
 * multipath_map_get_pgfailback: (skip)
 * @object: A #multipathMap.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map.pgfailback">"pgfailback"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
multipath_map_get_pgfailback (multipathMap *object)
{
  return MULTIPATH_MAP_GET_IFACE (object)->get_pgfailback (object);
}

/**
 * multipath_map_set_pgfailback: (skip)
 * @object: A #multipathMap.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map.pgfailback">"pgfailback"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_set_pgfailback (multipathMap *object, guint value)
{
  g_object_set (G_OBJECT (object), "pgfailback", value, NULL);
}

/**
 * multipath_map_get_pg_policy: (skip)
 * @object: A #multipathMap.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map.pg_policy">"pg_policy"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
multipath_map_get_pg_policy (multipathMap *object)
{
  return MULTIPATH_MAP_GET_IFACE (object)->get_pg_policy (object);
}

/**
 * multipath_map_set_pg_policy: (skip)
 * @object: A #multipathMap.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map.pg_policy">"pg_policy"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_set_pg_policy (multipathMap *object, guint value)
{
  g_object_set (G_OBJECT (object), "pg-policy", value, NULL);
}

/**
 * multipath_map_get_nextpg: (skip)
 * @object: A #multipathMap.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map.nextpg">"nextpg"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
multipath_map_get_nextpg (multipathMap *object)
{
  return MULTIPATH_MAP_GET_IFACE (object)->get_nextpg (object);
}

/**
 * multipath_map_set_nextpg: (skip)
 * @object: A #multipathMap.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map.nextpg">"nextpg"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_set_nextpg (multipathMap *object, gint value)
{
  g_object_set (G_OBJECT (object), "nextpg", value, NULL);
}

/**
 * multipath_map_get_bestpg: (skip)
 * @object: A #multipathMap.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map.bestpg">"bestpg"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
multipath_map_get_bestpg (multipathMap *object)
{
  return MULTIPATH_MAP_GET_IFACE (object)->get_bestpg (object);
}

/**
 * multipath_map_set_bestpg: (skip)
 * @object: A #multipathMap.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map.bestpg">"bestpg"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_set_bestpg (multipathMap *object, gint value)
{
  g_object_set (G_OBJECT (object), "bestpg", value, NULL);
}

/**
 * multipath_map_get_queuedio: (skip)
 * @object: A #multipathMap.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map.queuedio">"queuedio"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
multipath_map_get_queuedio (multipathMap *object)
{
  return MULTIPATH_MAP_GET_IFACE (object)->get_queuedio (object);
}

/**
 * multipath_map_set_queuedio: (skip)
 * @object: A #multipathMap.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map.queuedio">"queuedio"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_set_queuedio (multipathMap *object, gint value)
{
  g_object_set (G_OBJECT (object), "queuedio", value, NULL);
}

/**
 * multipath_map_get_no_path_retry: (skip)
 * @object: A #multipathMap.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map.no_path_retry">"no_path_retry"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
multipath_map_get_no_path_retry (multipathMap *object)
{
  return MULTIPATH_MAP_GET_IFACE (object)->get_no_path_retry (object);
}

/**
 * multipath_map_set_no_path_retry: (skip)
 * @object: A #multipathMap.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map.no_path_retry">"no_path_retry"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_set_no_path_retry (multipathMap *object, gint value)
{
  g_object_set (G_OBJECT (object), "no-path-retry", value, NULL);
}

/**
 * multipath_map_get_nr_active: (skip)
 * @object: A #multipathMap.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map.nr_active">"nr_active"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
multipath_map_get_nr_active (multipathMap *object)
{
  return MULTIPATH_MAP_GET_IFACE (object)->get_nr_active (object);
}

/**
 * multipath_map_set_nr_active: (skip)
 * @object: A #multipathMap.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map.nr_active">"nr_active"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_set_nr_active (multipathMap *object, guint value)
{
  g_object_set (G_OBJECT (object), "nr-active", value, NULL);
}

/**
 * multipath_map_get_selector: (skip)
 * @object: A #multipathMap.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map.selector">"selector"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use multipath_map_dup_selector() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
multipath_map_get_selector (multipathMap *object)
{
  return MULTIPATH_MAP_GET_IFACE (object)->get_selector (object);
}

/**
 * multipath_map_dup_selector: (skip)
 * @object: A #multipathMap.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-redhat-multipath-map.selector">"selector"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
multipath_map_dup_selector (multipathMap *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "selector", &value, NULL);
  return value;
}

/**
 * multipath_map_set_selector: (skip)
 * @object: A #multipathMap.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map.selector">"selector"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_set_selector (multipathMap *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "selector", value, NULL);
}

/**
 * multipath_map_get_paths: (skip)
 * @object: A #multipathMap.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map.paths">"paths"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use multipath_map_dup_paths() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
multipath_map_get_paths (multipathMap *object)
{
  return MULTIPATH_MAP_GET_IFACE (object)->get_paths (object);
}

/**
 * multipath_map_dup_paths: (skip)
 * @object: A #multipathMap.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-redhat-multipath-map.paths">"paths"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
multipath_map_dup_paths (multipathMap *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "paths", &value, NULL);
  return value;
}

/**
 * multipath_map_set_paths: (skip)
 * @object: A #multipathMap.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map.paths">"paths"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_set_paths (multipathMap *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "paths", value, NULL);
}

/**
 * multipath_map_get_pathgroups: (skip)
 * @object: A #multipathMap.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map.pathgroups">"pathgroups"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use multipath_map_dup_pathgroups() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
multipath_map_get_pathgroups (multipathMap *object)
{
  return MULTIPATH_MAP_GET_IFACE (object)->get_pathgroups (object);
}

/**
 * multipath_map_dup_pathgroups: (skip)
 * @object: A #multipathMap.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-redhat-multipath-map.pathgroups">"pathgroups"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
multipath_map_dup_pathgroups (multipathMap *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "pathgroups", &value, NULL);
  return value;
}

/**
 * multipath_map_set_pathgroups: (skip)
 * @object: A #multipathMap.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map.pathgroups">"pathgroups"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_set_pathgroups (multipathMap *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "pathgroups", value, NULL);
}

/* ------------------------------------------------------------------------ */

/**
 * multipathMapProxy:
 *
 * The #multipathMapProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * multipathMapProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #multipathMapProxy.
 */

struct _multipathMapProxyPrivate
{
  GData *qdata;
};

static void multipath_map_proxy_iface_init (multipathMapIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (multipathMapProxy, multipath_map_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (multipathMapProxy)
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_MAP, multipath_map_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (multipathMapProxy, multipath_map_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_MAP, multipath_map_proxy_iface_init));

#endif
static void
multipath_map_proxy_finalize (GObject *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (multipath_map_proxy_parent_class)->finalize (object);
}

static void
multipath_map_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 13);
  info = _multipath_map_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
multipath_map_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface com.redhat.multipath.map: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
multipath_map_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 13);
  info = _multipath_map_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "com.redhat.multipath.map", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) multipath_map_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
multipath_map_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_multipath_map_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], MULTIPATH_TYPE_MAP);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, MULTIPATH_TYPE_MAP);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
multipath_map_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_multipath_map_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_multipath_map_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
multipath_map_proxy_get_uuid (multipathMap *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "uuid");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
multipath_map_proxy_get_size (multipathMap *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "size");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
multipath_map_proxy_get_features (multipathMap *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "features");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
multipath_map_proxy_get_pgfailback (multipathMap *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "pgfailback");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
multipath_map_proxy_get_pg_policy (multipathMap *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "pg_policy");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint 
multipath_map_proxy_get_nextpg (multipathMap *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "nextpg");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint 
multipath_map_proxy_get_bestpg (multipathMap *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "bestpg");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint 
multipath_map_proxy_get_queuedio (multipathMap *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "queuedio");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint 
multipath_map_proxy_get_no_path_retry (multipathMap *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "no_path_retry");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
multipath_map_proxy_get_nr_active (multipathMap *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "nr_active");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
multipath_map_proxy_get_selector (multipathMap *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "selector");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
multipath_map_proxy_get_paths (multipathMap *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "paths");
  if (variant != NULL)
    {
      value = g_variant_get_objv (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
multipath_map_proxy_get_pathgroups (multipathMap *object)
{
  multipathMapProxy *proxy = MULTIPATH_MAP_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "pathgroups");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static void
multipath_map_proxy_init (multipathMapProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = multipath_map_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, MULTIPATH_TYPE_MAP_PROXY, multipathMapProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), multipath_map_interface_info ());
}

static void
multipath_map_proxy_class_init (multipathMapProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = multipath_map_proxy_finalize;
  gobject_class->get_property = multipath_map_proxy_get_property;
  gobject_class->set_property = multipath_map_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = multipath_map_proxy_g_signal;
  proxy_class->g_properties_changed = multipath_map_proxy_g_properties_changed;

  multipath_map_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (multipathMapProxyPrivate));
#endif
}

static void
multipath_map_proxy_iface_init (multipathMapIface *iface)
{
  iface->get_uuid = multipath_map_proxy_get_uuid;
  iface->get_size = multipath_map_proxy_get_size;
  iface->get_features = multipath_map_proxy_get_features;
  iface->get_pgfailback = multipath_map_proxy_get_pgfailback;
  iface->get_pg_policy = multipath_map_proxy_get_pg_policy;
  iface->get_nextpg = multipath_map_proxy_get_nextpg;
  iface->get_bestpg = multipath_map_proxy_get_bestpg;
  iface->get_queuedio = multipath_map_proxy_get_queuedio;
  iface->get_no_path_retry = multipath_map_proxy_get_no_path_retry;
  iface->get_nr_active = multipath_map_proxy_get_nr_active;
  iface->get_selector = multipath_map_proxy_get_selector;
  iface->get_paths = multipath_map_proxy_get_paths;
  iface->get_pathgroups = multipath_map_proxy_get_pathgroups;
}

/**
 * multipath_map_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map.top_of_page">com.redhat.multipath.map</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call multipath_map_proxy_new_finish() to get the result of the operation.
 *
 * See multipath_map_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
multipath_map_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (MULTIPATH_TYPE_MAP_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.redhat.multipath.map", NULL);
}

/**
 * multipath_map_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to multipath_map_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with multipath_map_proxy_new().
 *
 * Returns: (transfer full) (type multipathMapProxy): The constructed proxy object or %NULL if @error is set.
 */
multipathMap *
multipath_map_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return MULTIPATH_MAP (ret);
  else
    return NULL;
}

/**
 * multipath_map_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map.top_of_page">com.redhat.multipath.map</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See multipath_map_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type multipathMapProxy): The constructed proxy object or %NULL if @error is set.
 */
multipathMap *
multipath_map_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (MULTIPATH_TYPE_MAP_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.redhat.multipath.map", NULL);
  if (ret != NULL)
    return MULTIPATH_MAP (ret);
  else
    return NULL;
}


/**
 * multipath_map_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like multipath_map_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call multipath_map_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See multipath_map_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
multipath_map_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (MULTIPATH_TYPE_MAP_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.redhat.multipath.map", NULL);
}

/**
 * multipath_map_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to multipath_map_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with multipath_map_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type multipathMapProxy): The constructed proxy object or %NULL if @error is set.
 */
multipathMap *
multipath_map_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return MULTIPATH_MAP (ret);
  else
    return NULL;
}

/**
 * multipath_map_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like multipath_map_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See multipath_map_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type multipathMapProxy): The constructed proxy object or %NULL if @error is set.
 */
multipathMap *
multipath_map_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (MULTIPATH_TYPE_MAP_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.redhat.multipath.map", NULL);
  if (ret != NULL)
    return MULTIPATH_MAP (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * multipathMapSkeleton:
 *
 * The #multipathMapSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * multipathMapSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #multipathMapSkeleton.
 */

struct _multipathMapSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_multipath_map_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], MULTIPATH_TYPE_MAP);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, MULTIPATH_TYPE_MAP);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_multipath_map_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_multipath_map_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_multipath_map_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_multipath_map_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _multipath_map_skeleton_vtable =
{
  _multipath_map_skeleton_handle_method_call,
  _multipath_map_skeleton_handle_get_property,
  _multipath_map_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
multipath_map_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return multipath_map_interface_info ();
}

static GDBusInterfaceVTable *
multipath_map_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_multipath_map_skeleton_vtable;
}

static GVariant *
multipath_map_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_multipath_map_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _multipath_map_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _multipath_map_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _multipath_map_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.redhat.multipath.map", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _multipath_map_emit_changed (gpointer user_data);

static void
multipath_map_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _multipath_map_emit_changed (skeleton);
}

static void multipath_map_skeleton_iface_init (multipathMapIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (multipathMapSkeleton, multipath_map_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (multipathMapSkeleton)
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_MAP, multipath_map_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (multipathMapSkeleton, multipath_map_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_MAP, multipath_map_skeleton_iface_init));

#endif
static void
multipath_map_skeleton_finalize (GObject *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  guint n;
  for (n = 0; n < 13; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (multipath_map_skeleton_parent_class)->finalize (object);
}

static void
multipath_map_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 13);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_multipath_map_emit_changed (gpointer user_data)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "com.redhat.multipath.map",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_multipath_map_schedule_emit_changed (multipathMapSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
multipath_map_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _multipath_map_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _multipath_map_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
multipath_map_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 13);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _multipath_map_schedule_emit_changed (skeleton, _multipath_map_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
multipath_map_skeleton_init (multipathMapSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = multipath_map_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, MULTIPATH_TYPE_MAP_SKELETON, multipathMapSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 13);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[8], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[9], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[10], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[11], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[12], G_TYPE_STRING);
}

static const gchar *
multipath_map_skeleton_get_uuid (multipathMap *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
multipath_map_skeleton_get_size (multipathMap *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
multipath_map_skeleton_get_features (multipathMap *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
multipath_map_skeleton_get_pgfailback (multipathMap *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
multipath_map_skeleton_get_pg_policy (multipathMap *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint 
multipath_map_skeleton_get_nextpg (multipathMap *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint 
multipath_map_skeleton_get_bestpg (multipathMap *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint 
multipath_map_skeleton_get_queuedio (multipathMap *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint 
multipath_map_skeleton_get_no_path_retry (multipathMap *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[8]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
multipath_map_skeleton_get_nr_active (multipathMap *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[9]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
multipath_map_skeleton_get_selector (multipathMap *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[10]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
multipath_map_skeleton_get_paths (multipathMap *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[11]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
multipath_map_skeleton_get_pathgroups (multipathMap *object)
{
  multipathMapSkeleton *skeleton = MULTIPATH_MAP_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[12]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
multipath_map_skeleton_class_init (multipathMapSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = multipath_map_skeleton_finalize;
  gobject_class->get_property = multipath_map_skeleton_get_property;
  gobject_class->set_property = multipath_map_skeleton_set_property;
  gobject_class->notify       = multipath_map_skeleton_notify;


  multipath_map_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = multipath_map_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = multipath_map_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = multipath_map_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = multipath_map_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (multipathMapSkeletonPrivate));
#endif
}

static void
multipath_map_skeleton_iface_init (multipathMapIface *iface)
{
  iface->get_uuid = multipath_map_skeleton_get_uuid;
  iface->get_size = multipath_map_skeleton_get_size;
  iface->get_features = multipath_map_skeleton_get_features;
  iface->get_pgfailback = multipath_map_skeleton_get_pgfailback;
  iface->get_pg_policy = multipath_map_skeleton_get_pg_policy;
  iface->get_nextpg = multipath_map_skeleton_get_nextpg;
  iface->get_bestpg = multipath_map_skeleton_get_bestpg;
  iface->get_queuedio = multipath_map_skeleton_get_queuedio;
  iface->get_no_path_retry = multipath_map_skeleton_get_no_path_retry;
  iface->get_nr_active = multipath_map_skeleton_get_nr_active;
  iface->get_selector = multipath_map_skeleton_get_selector;
  iface->get_paths = multipath_map_skeleton_get_paths;
  iface->get_pathgroups = multipath_map_skeleton_get_pathgroups;
}

/**
 * multipath_map_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map.top_of_page">com.redhat.multipath.map</link>.
 *
 * Returns: (transfer full) (type multipathMapSkeleton): The skeleton object.
 */
multipathMap *
multipath_map_skeleton_new (void)
{
  return MULTIPATH_MAP (g_object_new (MULTIPATH_TYPE_MAP_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.redhat.multipath.map.path
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:multipathMapPath
 * @title: multipathMapPath
 * @short_description: Generated C code for the com.redhat.multipath.map.path D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-redhat-multipath-map-path.top_of_page">com.redhat.multipath.map.path</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.redhat.multipath.map.path ---- */

static const _ExtendedGDBusPropertyInfo _multipath_map_path_property_info_state =
{
  {
    -1,
    (gchar *) "state",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "state",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_path_property_info_dmstate =
{
  {
    -1,
    (gchar *) "dmstate",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "dmstate",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_path_property_info_offline =
{
  {
    -1,
    (gchar *) "offline",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "offline",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_path_property_info_bus =
{
  {
    -1,
    (gchar *) "bus",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "bus",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_path_property_info_dev =
{
  {
    -1,
    (gchar *) "dev",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "dev",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_path_property_info_vendor_id =
{
  {
    -1,
    (gchar *) "vendor_id",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "vendor-id",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_path_property_info_product_id =
{
  {
    -1,
    (gchar *) "product_id",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "product-id",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_path_property_info_rev =
{
  {
    -1,
    (gchar *) "rev",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "rev",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_path_property_info_serial =
{
  {
    -1,
    (gchar *) "serial",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "serial",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _multipath_map_path_property_info_tgt_node_name =
{
  {
    -1,
    (gchar *) "tgt_node_name",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "tgt-node-name",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _multipath_map_path_property_info_pointers[] =
{
  &_multipath_map_path_property_info_state,
  &_multipath_map_path_property_info_dmstate,
  &_multipath_map_path_property_info_offline,
  &_multipath_map_path_property_info_bus,
  &_multipath_map_path_property_info_dev,
  &_multipath_map_path_property_info_vendor_id,
  &_multipath_map_path_property_info_product_id,
  &_multipath_map_path_property_info_rev,
  &_multipath_map_path_property_info_serial,
  &_multipath_map_path_property_info_tgt_node_name,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _multipath_map_path_interface_info =
{
  {
    -1,
    (gchar *) "com.redhat.multipath.map.path",
    NULL,
    NULL,
    (GDBusPropertyInfo **) &_multipath_map_path_property_info_pointers,
    NULL
  },
  "map-path",
};


/**
 * multipath_map_path_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-redhat-multipath-map-path.top_of_page">com.redhat.multipath.map.path</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
multipath_map_path_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_multipath_map_path_interface_info.parent_struct;
}

/**
 * multipath_map_path_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #multipathMapPath interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
multipath_map_path_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "state");
  g_object_class_override_property (klass, property_id_begin++, "dmstate");
  g_object_class_override_property (klass, property_id_begin++, "offline");
  g_object_class_override_property (klass, property_id_begin++, "bus");
  g_object_class_override_property (klass, property_id_begin++, "dev");
  g_object_class_override_property (klass, property_id_begin++, "vendor-id");
  g_object_class_override_property (klass, property_id_begin++, "product-id");
  g_object_class_override_property (klass, property_id_begin++, "rev");
  g_object_class_override_property (klass, property_id_begin++, "serial");
  g_object_class_override_property (klass, property_id_begin++, "tgt-node-name");
  return property_id_begin - 1;
}



/**
 * multipathMapPath:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map-path.top_of_page">com.redhat.multipath.map.path</link>.
 */

/**
 * multipathMapPathIface:
 * @parent_iface: The parent interface.
 * @get_bus: Getter for the #multipathMapPath:bus property.
 * @get_dev: Getter for the #multipathMapPath:dev property.
 * @get_dmstate: Getter for the #multipathMapPath:dmstate property.
 * @get_offline: Getter for the #multipathMapPath:offline property.
 * @get_product_id: Getter for the #multipathMapPath:product-id property.
 * @get_rev: Getter for the #multipathMapPath:rev property.
 * @get_serial: Getter for the #multipathMapPath:serial property.
 * @get_state: Getter for the #multipathMapPath:state property.
 * @get_tgt_node_name: Getter for the #multipathMapPath:tgt-node-name property.
 * @get_vendor_id: Getter for the #multipathMapPath:vendor-id property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map-path.top_of_page">com.redhat.multipath.map.path</link>.
 */

typedef multipathMapPathIface multipathMapPathInterface;
G_DEFINE_INTERFACE (multipathMapPath, multipath_map_path, G_TYPE_OBJECT);

static void
multipath_map_path_default_init (multipathMapPathIface *iface)
{
  /* GObject properties for D-Bus properties: */
  /**
   * multipathMapPath:state:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map-path.state">"state"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("state", "state", "state", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMapPath:dmstate:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map-path.dmstate">"dmstate"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("dmstate", "dmstate", "dmstate", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMapPath:offline:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map-path.offline">"offline"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("offline", "offline", "offline", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMapPath:bus:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map-path.bus">"bus"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("bus", "bus", "bus", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMapPath:dev:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map-path.dev">"dev"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("dev", "dev", "dev", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMapPath:vendor-id:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map-path.vendor_id">"vendor_id"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("vendor-id", "vendor_id", "vendor_id", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMapPath:product-id:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map-path.product_id">"product_id"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("product-id", "product_id", "product_id", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMapPath:rev:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map-path.rev">"rev"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("rev", "rev", "rev", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMapPath:serial:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map-path.serial">"serial"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("serial", "serial", "serial", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * multipathMapPath:tgt-node-name:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-redhat-multipath-map-path.tgt_node_name">"tgt_node_name"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("tgt-node-name", "tgt_node_name", "tgt_node_name", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * multipath_map_path_get_state: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map-path.state">"state"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
multipath_map_path_get_state (multipathMapPath *object)
{
  return MULTIPATH_MAP_PATH_GET_IFACE (object)->get_state (object);
}

/**
 * multipath_map_path_set_state: (skip)
 * @object: A #multipathMapPath.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map-path.state">"state"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_path_set_state (multipathMapPath *object, guint value)
{
  g_object_set (G_OBJECT (object), "state", value, NULL);
}

/**
 * multipath_map_path_get_dmstate: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map-path.dmstate">"dmstate"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
multipath_map_path_get_dmstate (multipathMapPath *object)
{
  return MULTIPATH_MAP_PATH_GET_IFACE (object)->get_dmstate (object);
}

/**
 * multipath_map_path_set_dmstate: (skip)
 * @object: A #multipathMapPath.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map-path.dmstate">"dmstate"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_path_set_dmstate (multipathMapPath *object, guint value)
{
  g_object_set (G_OBJECT (object), "dmstate", value, NULL);
}

/**
 * multipath_map_path_get_offline: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map-path.offline">"offline"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
multipath_map_path_get_offline (multipathMapPath *object)
{
  return MULTIPATH_MAP_PATH_GET_IFACE (object)->get_offline (object);
}

/**
 * multipath_map_path_set_offline: (skip)
 * @object: A #multipathMapPath.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map-path.offline">"offline"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_path_set_offline (multipathMapPath *object, guint value)
{
  g_object_set (G_OBJECT (object), "offline", value, NULL);
}

/**
 * multipath_map_path_get_bus: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map-path.bus">"bus"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
multipath_map_path_get_bus (multipathMapPath *object)
{
  return MULTIPATH_MAP_PATH_GET_IFACE (object)->get_bus (object);
}

/**
 * multipath_map_path_set_bus: (skip)
 * @object: A #multipathMapPath.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map-path.bus">"bus"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_path_set_bus (multipathMapPath *object, guint value)
{
  g_object_set (G_OBJECT (object), "bus", value, NULL);
}

/**
 * multipath_map_path_get_dev: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map-path.dev">"dev"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use multipath_map_path_dup_dev() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
multipath_map_path_get_dev (multipathMapPath *object)
{
  return MULTIPATH_MAP_PATH_GET_IFACE (object)->get_dev (object);
}

/**
 * multipath_map_path_dup_dev: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-redhat-multipath-map-path.dev">"dev"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
multipath_map_path_dup_dev (multipathMapPath *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "dev", &value, NULL);
  return value;
}

/**
 * multipath_map_path_set_dev: (skip)
 * @object: A #multipathMapPath.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map-path.dev">"dev"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_path_set_dev (multipathMapPath *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "dev", value, NULL);
}

/**
 * multipath_map_path_get_vendor_id: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map-path.vendor_id">"vendor_id"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use multipath_map_path_dup_vendor_id() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
multipath_map_path_get_vendor_id (multipathMapPath *object)
{
  return MULTIPATH_MAP_PATH_GET_IFACE (object)->get_vendor_id (object);
}

/**
 * multipath_map_path_dup_vendor_id: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-redhat-multipath-map-path.vendor_id">"vendor_id"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
multipath_map_path_dup_vendor_id (multipathMapPath *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "vendor-id", &value, NULL);
  return value;
}

/**
 * multipath_map_path_set_vendor_id: (skip)
 * @object: A #multipathMapPath.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map-path.vendor_id">"vendor_id"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_path_set_vendor_id (multipathMapPath *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "vendor-id", value, NULL);
}

/**
 * multipath_map_path_get_product_id: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map-path.product_id">"product_id"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use multipath_map_path_dup_product_id() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
multipath_map_path_get_product_id (multipathMapPath *object)
{
  return MULTIPATH_MAP_PATH_GET_IFACE (object)->get_product_id (object);
}

/**
 * multipath_map_path_dup_product_id: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-redhat-multipath-map-path.product_id">"product_id"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
multipath_map_path_dup_product_id (multipathMapPath *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "product-id", &value, NULL);
  return value;
}

/**
 * multipath_map_path_set_product_id: (skip)
 * @object: A #multipathMapPath.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map-path.product_id">"product_id"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_path_set_product_id (multipathMapPath *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "product-id", value, NULL);
}

/**
 * multipath_map_path_get_rev: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map-path.rev">"rev"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use multipath_map_path_dup_rev() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
multipath_map_path_get_rev (multipathMapPath *object)
{
  return MULTIPATH_MAP_PATH_GET_IFACE (object)->get_rev (object);
}

/**
 * multipath_map_path_dup_rev: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-redhat-multipath-map-path.rev">"rev"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
multipath_map_path_dup_rev (multipathMapPath *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "rev", &value, NULL);
  return value;
}

/**
 * multipath_map_path_set_rev: (skip)
 * @object: A #multipathMapPath.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map-path.rev">"rev"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_path_set_rev (multipathMapPath *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "rev", value, NULL);
}

/**
 * multipath_map_path_get_serial: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map-path.serial">"serial"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use multipath_map_path_dup_serial() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
multipath_map_path_get_serial (multipathMapPath *object)
{
  return MULTIPATH_MAP_PATH_GET_IFACE (object)->get_serial (object);
}

/**
 * multipath_map_path_dup_serial: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-redhat-multipath-map-path.serial">"serial"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
multipath_map_path_dup_serial (multipathMapPath *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "serial", &value, NULL);
  return value;
}

/**
 * multipath_map_path_set_serial: (skip)
 * @object: A #multipathMapPath.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map-path.serial">"serial"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_path_set_serial (multipathMapPath *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "serial", value, NULL);
}

/**
 * multipath_map_path_get_tgt_node_name: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets the value of the <link linkend="gdbus-property-com-redhat-multipath-map-path.tgt_node_name">"tgt_node_name"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use multipath_map_path_dup_tgt_node_name() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
multipath_map_path_get_tgt_node_name (multipathMapPath *object)
{
  return MULTIPATH_MAP_PATH_GET_IFACE (object)->get_tgt_node_name (object);
}

/**
 * multipath_map_path_dup_tgt_node_name: (skip)
 * @object: A #multipathMapPath.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-redhat-multipath-map-path.tgt_node_name">"tgt_node_name"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
multipath_map_path_dup_tgt_node_name (multipathMapPath *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "tgt-node-name", &value, NULL);
  return value;
}

/**
 * multipath_map_path_set_tgt_node_name: (skip)
 * @object: A #multipathMapPath.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-redhat-multipath-map-path.tgt_node_name">"tgt_node_name"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
multipath_map_path_set_tgt_node_name (multipathMapPath *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "tgt-node-name", value, NULL);
}

/* ------------------------------------------------------------------------ */

/**
 * multipathMapPathProxy:
 *
 * The #multipathMapPathProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * multipathMapPathProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #multipathMapPathProxy.
 */

struct _multipathMapPathProxyPrivate
{
  GData *qdata;
};

static void multipath_map_path_proxy_iface_init (multipathMapPathIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (multipathMapPathProxy, multipath_map_path_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (multipathMapPathProxy)
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_MAP_PATH, multipath_map_path_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (multipathMapPathProxy, multipath_map_path_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_MAP_PATH, multipath_map_path_proxy_iface_init));

#endif
static void
multipath_map_path_proxy_finalize (GObject *object)
{
  multipathMapPathProxy *proxy = MULTIPATH_MAP_PATH_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (multipath_map_path_proxy_parent_class)->finalize (object);
}

static void
multipath_map_path_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 10);
  info = _multipath_map_path_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
multipath_map_path_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface com.redhat.multipath.map.path: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
multipath_map_path_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 10);
  info = _multipath_map_path_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "com.redhat.multipath.map.path", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) multipath_map_path_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
multipath_map_path_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_multipath_map_path_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], MULTIPATH_TYPE_MAP_PATH);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, MULTIPATH_TYPE_MAP_PATH);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
multipath_map_path_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  multipathMapPathProxy *proxy = MULTIPATH_MAP_PATH_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_multipath_map_path_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_multipath_map_path_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static guint 
multipath_map_path_proxy_get_state (multipathMapPath *object)
{
  multipathMapPathProxy *proxy = MULTIPATH_MAP_PATH_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "state");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
multipath_map_path_proxy_get_dmstate (multipathMapPath *object)
{
  multipathMapPathProxy *proxy = MULTIPATH_MAP_PATH_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "dmstate");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
multipath_map_path_proxy_get_offline (multipathMapPath *object)
{
  multipathMapPathProxy *proxy = MULTIPATH_MAP_PATH_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "offline");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
multipath_map_path_proxy_get_bus (multipathMapPath *object)
{
  multipathMapPathProxy *proxy = MULTIPATH_MAP_PATH_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "bus");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
multipath_map_path_proxy_get_dev (multipathMapPath *object)
{
  multipathMapPathProxy *proxy = MULTIPATH_MAP_PATH_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "dev");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
multipath_map_path_proxy_get_vendor_id (multipathMapPath *object)
{
  multipathMapPathProxy *proxy = MULTIPATH_MAP_PATH_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "vendor_id");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
multipath_map_path_proxy_get_product_id (multipathMapPath *object)
{
  multipathMapPathProxy *proxy = MULTIPATH_MAP_PATH_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "product_id");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
multipath_map_path_proxy_get_rev (multipathMapPath *object)
{
  multipathMapPathProxy *proxy = MULTIPATH_MAP_PATH_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "rev");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
multipath_map_path_proxy_get_serial (multipathMapPath *object)
{
  multipathMapPathProxy *proxy = MULTIPATH_MAP_PATH_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "serial");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
multipath_map_path_proxy_get_tgt_node_name (multipathMapPath *object)
{
  multipathMapPathProxy *proxy = MULTIPATH_MAP_PATH_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "tgt_node_name");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static void
multipath_map_path_proxy_init (multipathMapPathProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = multipath_map_path_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, MULTIPATH_TYPE_MAP_PATH_PROXY, multipathMapPathProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), multipath_map_path_interface_info ());
}

static void
multipath_map_path_proxy_class_init (multipathMapPathProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = multipath_map_path_proxy_finalize;
  gobject_class->get_property = multipath_map_path_proxy_get_property;
  gobject_class->set_property = multipath_map_path_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = multipath_map_path_proxy_g_signal;
  proxy_class->g_properties_changed = multipath_map_path_proxy_g_properties_changed;

  multipath_map_path_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (multipathMapPathProxyPrivate));
#endif
}

static void
multipath_map_path_proxy_iface_init (multipathMapPathIface *iface)
{
  iface->get_state = multipath_map_path_proxy_get_state;
  iface->get_dmstate = multipath_map_path_proxy_get_dmstate;
  iface->get_offline = multipath_map_path_proxy_get_offline;
  iface->get_bus = multipath_map_path_proxy_get_bus;
  iface->get_dev = multipath_map_path_proxy_get_dev;
  iface->get_vendor_id = multipath_map_path_proxy_get_vendor_id;
  iface->get_product_id = multipath_map_path_proxy_get_product_id;
  iface->get_rev = multipath_map_path_proxy_get_rev;
  iface->get_serial = multipath_map_path_proxy_get_serial;
  iface->get_tgt_node_name = multipath_map_path_proxy_get_tgt_node_name;
}

/**
 * multipath_map_path_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map-path.top_of_page">com.redhat.multipath.map.path</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call multipath_map_path_proxy_new_finish() to get the result of the operation.
 *
 * See multipath_map_path_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
multipath_map_path_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (MULTIPATH_TYPE_MAP_PATH_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.redhat.multipath.map.path", NULL);
}

/**
 * multipath_map_path_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to multipath_map_path_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with multipath_map_path_proxy_new().
 *
 * Returns: (transfer full) (type multipathMapPathProxy): The constructed proxy object or %NULL if @error is set.
 */
multipathMapPath *
multipath_map_path_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return MULTIPATH_MAP_PATH (ret);
  else
    return NULL;
}

/**
 * multipath_map_path_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map-path.top_of_page">com.redhat.multipath.map.path</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See multipath_map_path_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type multipathMapPathProxy): The constructed proxy object or %NULL if @error is set.
 */
multipathMapPath *
multipath_map_path_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (MULTIPATH_TYPE_MAP_PATH_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.redhat.multipath.map.path", NULL);
  if (ret != NULL)
    return MULTIPATH_MAP_PATH (ret);
  else
    return NULL;
}


/**
 * multipath_map_path_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like multipath_map_path_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call multipath_map_path_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See multipath_map_path_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
multipath_map_path_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (MULTIPATH_TYPE_MAP_PATH_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.redhat.multipath.map.path", NULL);
}

/**
 * multipath_map_path_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to multipath_map_path_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with multipath_map_path_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type multipathMapPathProxy): The constructed proxy object or %NULL if @error is set.
 */
multipathMapPath *
multipath_map_path_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return MULTIPATH_MAP_PATH (ret);
  else
    return NULL;
}

/**
 * multipath_map_path_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like multipath_map_path_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See multipath_map_path_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type multipathMapPathProxy): The constructed proxy object or %NULL if @error is set.
 */
multipathMapPath *
multipath_map_path_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (MULTIPATH_TYPE_MAP_PATH_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.redhat.multipath.map.path", NULL);
  if (ret != NULL)
    return MULTIPATH_MAP_PATH (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * multipathMapPathSkeleton:
 *
 * The #multipathMapPathSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * multipathMapPathSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #multipathMapPathSkeleton.
 */

struct _multipathMapPathSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_multipath_map_path_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], MULTIPATH_TYPE_MAP_PATH);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, MULTIPATH_TYPE_MAP_PATH);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_multipath_map_path_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_multipath_map_path_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_multipath_map_path_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_multipath_map_path_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _multipath_map_path_skeleton_vtable =
{
  _multipath_map_path_skeleton_handle_method_call,
  _multipath_map_path_skeleton_handle_get_property,
  _multipath_map_path_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
multipath_map_path_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return multipath_map_path_interface_info ();
}

static GDBusInterfaceVTable *
multipath_map_path_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_multipath_map_path_skeleton_vtable;
}

static GVariant *
multipath_map_path_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_multipath_map_path_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _multipath_map_path_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _multipath_map_path_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _multipath_map_path_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.redhat.multipath.map.path", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _multipath_map_path_emit_changed (gpointer user_data);

static void
multipath_map_path_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _multipath_map_path_emit_changed (skeleton);
}

static void multipath_map_path_skeleton_iface_init (multipathMapPathIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (multipathMapPathSkeleton, multipath_map_path_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (multipathMapPathSkeleton)
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_MAP_PATH, multipath_map_path_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (multipathMapPathSkeleton, multipath_map_path_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_MAP_PATH, multipath_map_path_skeleton_iface_init));

#endif
static void
multipath_map_path_skeleton_finalize (GObject *object)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  guint n;
  for (n = 0; n < 10; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (multipath_map_path_skeleton_parent_class)->finalize (object);
}

static void
multipath_map_path_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 10);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_multipath_map_path_emit_changed (gpointer user_data)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "com.redhat.multipath.map.path",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_multipath_map_path_schedule_emit_changed (multipathMapPathSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
multipath_map_path_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _multipath_map_path_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _multipath_map_path_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
multipath_map_path_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 10);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _multipath_map_path_schedule_emit_changed (skeleton, _multipath_map_path_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
multipath_map_path_skeleton_init (multipathMapPathSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = multipath_map_path_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, MULTIPATH_TYPE_MAP_PATH_SKELETON, multipathMapPathSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 10);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[8], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[9], G_TYPE_STRING);
}

static guint 
multipath_map_path_skeleton_get_state (multipathMapPath *object)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
multipath_map_path_skeleton_get_dmstate (multipathMapPath *object)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
multipath_map_path_skeleton_get_offline (multipathMapPath *object)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
multipath_map_path_skeleton_get_bus (multipathMapPath *object)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
multipath_map_path_skeleton_get_dev (multipathMapPath *object)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
multipath_map_path_skeleton_get_vendor_id (multipathMapPath *object)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
multipath_map_path_skeleton_get_product_id (multipathMapPath *object)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
multipath_map_path_skeleton_get_rev (multipathMapPath *object)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
multipath_map_path_skeleton_get_serial (multipathMapPath *object)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[8]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
multipath_map_path_skeleton_get_tgt_node_name (multipathMapPath *object)
{
  multipathMapPathSkeleton *skeleton = MULTIPATH_MAP_PATH_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[9]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
multipath_map_path_skeleton_class_init (multipathMapPathSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = multipath_map_path_skeleton_finalize;
  gobject_class->get_property = multipath_map_path_skeleton_get_property;
  gobject_class->set_property = multipath_map_path_skeleton_set_property;
  gobject_class->notify       = multipath_map_path_skeleton_notify;


  multipath_map_path_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = multipath_map_path_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = multipath_map_path_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = multipath_map_path_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = multipath_map_path_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (multipathMapPathSkeletonPrivate));
#endif
}

static void
multipath_map_path_skeleton_iface_init (multipathMapPathIface *iface)
{
  iface->get_state = multipath_map_path_skeleton_get_state;
  iface->get_dmstate = multipath_map_path_skeleton_get_dmstate;
  iface->get_offline = multipath_map_path_skeleton_get_offline;
  iface->get_bus = multipath_map_path_skeleton_get_bus;
  iface->get_dev = multipath_map_path_skeleton_get_dev;
  iface->get_vendor_id = multipath_map_path_skeleton_get_vendor_id;
  iface->get_product_id = multipath_map_path_skeleton_get_product_id;
  iface->get_rev = multipath_map_path_skeleton_get_rev;
  iface->get_serial = multipath_map_path_skeleton_get_serial;
  iface->get_tgt_node_name = multipath_map_path_skeleton_get_tgt_node_name;
}

/**
 * multipath_map_path_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map-path.top_of_page">com.redhat.multipath.map.path</link>.
 *
 * Returns: (transfer full) (type multipathMapPathSkeleton): The skeleton object.
 */
multipathMapPath *
multipath_map_path_skeleton_new (void)
{
  return MULTIPATH_MAP_PATH (g_object_new (MULTIPATH_TYPE_MAP_PATH_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for Object, ObjectProxy and ObjectSkeleton
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:multipathObject
 * @title: multipathObject
 * @short_description: Specialized GDBusObject types
 *
 * This section contains the #multipathObject, #multipathObjectProxy, and #multipathObjectSkeleton types which make it easier to work with objects implementing generated types for D-Bus interfaces.
 */

/**
 * multipathObject:
 *
 * The #multipathObject type is a specialized container of interfaces.
 */

/**
 * multipathObjectIface:
 * @parent_iface: The parent interface.
 *
 * Virtual table for the #multipathObject interface.
 */

typedef multipathObjectIface multipathObjectInterface;
G_DEFINE_INTERFACE_WITH_CODE (multipathObject, multipath_object, G_TYPE_OBJECT, g_type_interface_add_prerequisite (g_define_type_id, G_TYPE_DBUS_OBJECT));

static void
multipath_object_default_init (multipathObjectIface *iface)
{
  /**
   * multipathObject:manager:
   *
   * The #multipathManager instance corresponding to the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-Manager.top_of_page">com.redhat.multipath.Manager</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("manager", "manager", "manager", MULTIPATH_TYPE_MANAGER, G_PARAM_READWRITE|G_PARAM_STATIC_STRINGS));

  /**
   * multipathObject:map:
   *
   * The #multipathMap instance corresponding to the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map.top_of_page">com.redhat.multipath.map</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("map", "map", "map", MULTIPATH_TYPE_MAP, G_PARAM_READWRITE|G_PARAM_STATIC_STRINGS));

  /**
   * multipathObject:map-path:
   *
   * The #multipathMapPath instance corresponding to the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map-path.top_of_page">com.redhat.multipath.map.path</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("map-path", "map-path", "map-path", MULTIPATH_TYPE_MAP_PATH, G_PARAM_READWRITE|G_PARAM_STATIC_STRINGS));

}

/**
 * multipath_object_get_manager:
 * @object: A #multipathObject.
 *
 * Gets the #multipathManager instance for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-Manager.top_of_page">com.redhat.multipath.Manager</link> on @object, if any.
 *
 * Returns: (transfer full): A #multipathManager that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
multipathManager *multipath_object_get_manager (multipathObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "com.redhat.multipath.Manager");
  if (ret == NULL)
    return NULL;
  return MULTIPATH_MANAGER (ret);
}

/**
 * multipath_object_get_map:
 * @object: A #multipathObject.
 *
 * Gets the #multipathMap instance for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map.top_of_page">com.redhat.multipath.map</link> on @object, if any.
 *
 * Returns: (transfer full): A #multipathMap that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
multipathMap *multipath_object_get_map (multipathObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "com.redhat.multipath.map");
  if (ret == NULL)
    return NULL;
  return MULTIPATH_MAP (ret);
}

/**
 * multipath_object_get_map_path:
 * @object: A #multipathObject.
 *
 * Gets the #multipathMapPath instance for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map-path.top_of_page">com.redhat.multipath.map.path</link> on @object, if any.
 *
 * Returns: (transfer full): A #multipathMapPath that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
multipathMapPath *multipath_object_get_map_path (multipathObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "com.redhat.multipath.map.path");
  if (ret == NULL)
    return NULL;
  return MULTIPATH_MAP_PATH (ret);
}


/**
 * multipath_object_peek_manager: (skip)
 * @object: A #multipathObject.
 *
 * Like multipath_object_get_manager() but doesn't increase the reference count on the returned object.
 *
 * <warning>It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.</warning>
 *
 * Returns: (transfer none): A #multipathManager or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
multipathManager *multipath_object_peek_manager (multipathObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "com.redhat.multipath.Manager");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return MULTIPATH_MANAGER (ret);
}

/**
 * multipath_object_peek_map: (skip)
 * @object: A #multipathObject.
 *
 * Like multipath_object_get_map() but doesn't increase the reference count on the returned object.
 *
 * <warning>It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.</warning>
 *
 * Returns: (transfer none): A #multipathMap or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
multipathMap *multipath_object_peek_map (multipathObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "com.redhat.multipath.map");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return MULTIPATH_MAP (ret);
}

/**
 * multipath_object_peek_map_path: (skip)
 * @object: A #multipathObject.
 *
 * Like multipath_object_get_map_path() but doesn't increase the reference count on the returned object.
 *
 * <warning>It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.</warning>
 *
 * Returns: (transfer none): A #multipathMapPath or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
multipathMapPath *multipath_object_peek_map_path (multipathObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "com.redhat.multipath.map.path");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return MULTIPATH_MAP_PATH (ret);
}


static void
multipath_object_notify (GDBusObject *object, GDBusInterface *interface)
{
  _ExtendedGDBusInterfaceInfo *info = (_ExtendedGDBusInterfaceInfo *) g_dbus_interface_get_info (interface);
  /* info can be NULL if the other end is using a D-Bus interface we don't know
   * anything about, for example old generated code in this process talking to
   * newer generated code in the other process. */
  if (info != NULL)
    g_object_notify (G_OBJECT (object), info->hyphen_name);
}

/**
 * multipathObjectProxy:
 *
 * The #multipathObjectProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * multipathObjectProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #multipathObjectProxy.
 */

static void
multipath_object_proxy__multipath_object_iface_init (multipathObjectIface *iface G_GNUC_UNUSED)
{
}

static void
multipath_object_proxy__g_dbus_object_iface_init (GDBusObjectIface *iface)
{
  iface->interface_added = multipath_object_notify;
  iface->interface_removed = multipath_object_notify;
}


G_DEFINE_TYPE_WITH_CODE (multipathObjectProxy, multipath_object_proxy, G_TYPE_DBUS_OBJECT_PROXY,
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_OBJECT, multipath_object_proxy__multipath_object_iface_init)
                         G_IMPLEMENT_INTERFACE (G_TYPE_DBUS_OBJECT, multipath_object_proxy__g_dbus_object_iface_init));

static void
multipath_object_proxy_init (multipathObjectProxy *object G_GNUC_UNUSED)
{
}

static void
multipath_object_proxy_set_property (GObject      *gobject,
  guint         prop_id,
  const GValue *value G_GNUC_UNUSED,
  GParamSpec   *pspec)
{
  G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
}

static void
multipath_object_proxy_get_property (GObject      *gobject,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  multipathObjectProxy *object = MULTIPATH_OBJECT_PROXY (gobject);
  GDBusInterface *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "com.redhat.multipath.Manager");
      g_value_take_object (value, interface);
      break;

    case 2:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "com.redhat.multipath.map");
      g_value_take_object (value, interface);
      break;

    case 3:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "com.redhat.multipath.map.path");
      g_value_take_object (value, interface);
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
multipath_object_proxy_class_init (multipathObjectProxyClass *klass)
{
  GObjectClass *gobject_class = G_OBJECT_CLASS (klass);

  gobject_class->set_property = multipath_object_proxy_set_property;
  gobject_class->get_property = multipath_object_proxy_get_property;

  g_object_class_override_property (gobject_class, 1, "manager");
  g_object_class_override_property (gobject_class, 2, "map");
  g_object_class_override_property (gobject_class, 3, "map-path");
}

/**
 * multipath_object_proxy_new:
 * @connection: A #GDBusConnection.
 * @object_path: An object path.
 *
 * Creates a new proxy object.
 *
 * Returns: (transfer full): The proxy object.
 */
multipathObjectProxy *
multipath_object_proxy_new (GDBusConnection *connection,
  const gchar *object_path)
{
  g_return_val_if_fail (G_IS_DBUS_CONNECTION (connection), NULL);
  g_return_val_if_fail (g_variant_is_object_path (object_path), NULL);
  return MULTIPATH_OBJECT_PROXY (g_object_new (MULTIPATH_TYPE_OBJECT_PROXY, "g-connection", connection, "g-object-path", object_path, NULL));
}

/**
 * multipathObjectSkeleton:
 *
 * The #multipathObjectSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * multipathObjectSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #multipathObjectSkeleton.
 */

static void
multipath_object_skeleton__multipath_object_iface_init (multipathObjectIface *iface G_GNUC_UNUSED)
{
}


static void
multipath_object_skeleton__g_dbus_object_iface_init (GDBusObjectIface *iface)
{
  iface->interface_added = multipath_object_notify;
  iface->interface_removed = multipath_object_notify;
}

G_DEFINE_TYPE_WITH_CODE (multipathObjectSkeleton, multipath_object_skeleton, G_TYPE_DBUS_OBJECT_SKELETON,
                         G_IMPLEMENT_INTERFACE (MULTIPATH_TYPE_OBJECT, multipath_object_skeleton__multipath_object_iface_init)
                         G_IMPLEMENT_INTERFACE (G_TYPE_DBUS_OBJECT, multipath_object_skeleton__g_dbus_object_iface_init));

static void
multipath_object_skeleton_init (multipathObjectSkeleton *object G_GNUC_UNUSED)
{
}

static void
multipath_object_skeleton_set_property (GObject      *gobject,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  multipathObjectSkeleton *object = MULTIPATH_OBJECT_SKELETON (gobject);
  GDBusInterfaceSkeleton *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (MULTIPATH_IS_MANAGER (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "com.redhat.multipath.Manager");
        }
      break;

    case 2:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (MULTIPATH_IS_MAP (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "com.redhat.multipath.map");
        }
      break;

    case 3:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (MULTIPATH_IS_MAP_PATH (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "com.redhat.multipath.map.path");
        }
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
multipath_object_skeleton_get_property (GObject      *gobject,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  multipathObjectSkeleton *object = MULTIPATH_OBJECT_SKELETON (gobject);
  GDBusInterface *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "com.redhat.multipath.Manager");
      g_value_take_object (value, interface);
      break;

    case 2:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "com.redhat.multipath.map");
      g_value_take_object (value, interface);
      break;

    case 3:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "com.redhat.multipath.map.path");
      g_value_take_object (value, interface);
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
multipath_object_skeleton_class_init (multipathObjectSkeletonClass *klass)
{
  GObjectClass *gobject_class = G_OBJECT_CLASS (klass);

  gobject_class->set_property = multipath_object_skeleton_set_property;
  gobject_class->get_property = multipath_object_skeleton_get_property;

  g_object_class_override_property (gobject_class, 1, "manager");
  g_object_class_override_property (gobject_class, 2, "map");
  g_object_class_override_property (gobject_class, 3, "map-path");
}

/**
 * multipath_object_skeleton_new:
 * @object_path: An object path.
 *
 * Creates a new skeleton object.
 *
 * Returns: (transfer full): The skeleton object.
 */
multipathObjectSkeleton *
multipath_object_skeleton_new (const gchar *object_path)
{
  g_return_val_if_fail (g_variant_is_object_path (object_path), NULL);
  return MULTIPATH_OBJECT_SKELETON (g_object_new (MULTIPATH_TYPE_OBJECT_SKELETON, "g-object-path", object_path, NULL));
}

/**
 * multipath_object_skeleton_set_manager:
 * @object: A #multipathObjectSkeleton.
 * @interface_: (allow-none): A #multipathManager or %NULL to clear the interface.
 *
 * Sets the #multipathManager instance for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-Manager.top_of_page">com.redhat.multipath.Manager</link> on @object.
 */
void multipath_object_skeleton_set_manager (multipathObjectSkeleton *object, multipathManager *interface_)
{
  g_object_set (G_OBJECT (object), "manager", interface_, NULL);
}

/**
 * multipath_object_skeleton_set_map:
 * @object: A #multipathObjectSkeleton.
 * @interface_: (allow-none): A #multipathMap or %NULL to clear the interface.
 *
 * Sets the #multipathMap instance for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map.top_of_page">com.redhat.multipath.map</link> on @object.
 */
void multipath_object_skeleton_set_map (multipathObjectSkeleton *object, multipathMap *interface_)
{
  g_object_set (G_OBJECT (object), "map", interface_, NULL);
}

/**
 * multipath_object_skeleton_set_map_path:
 * @object: A #multipathObjectSkeleton.
 * @interface_: (allow-none): A #multipathMapPath or %NULL to clear the interface.
 *
 * Sets the #multipathMapPath instance for the D-Bus interface <link linkend="gdbus-interface-com-redhat-multipath-map-path.top_of_page">com.redhat.multipath.map.path</link> on @object.
 */
void multipath_object_skeleton_set_map_path (multipathObjectSkeleton *object, multipathMapPath *interface_)
{
  g_object_set (G_OBJECT (object), "map-path", interface_, NULL);
}


/* ------------------------------------------------------------------------
 * Code for ObjectManager client
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:multipathObjectManagerClient
 * @title: multipathObjectManagerClient
 * @short_description: Generated GDBusObjectManagerClient type
 *
 * This section contains a #GDBusObjectManagerClient that uses multipath_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc.
 */

/**
 * multipathObjectManagerClient:
 *
 * The #multipathObjectManagerClient structure contains only private data and should only be accessed using the provided API.
 */

/**
 * multipathObjectManagerClientClass:
 * @parent_class: The parent class.
 *
 * Class structure for #multipathObjectManagerClient.
 */

G_DEFINE_TYPE (multipathObjectManagerClient, multipath_object_manager_client, G_TYPE_DBUS_OBJECT_MANAGER_CLIENT);

static void
multipath_object_manager_client_init (multipathObjectManagerClient *manager G_GNUC_UNUSED)
{
}

static void
multipath_object_manager_client_class_init (multipathObjectManagerClientClass *klass G_GNUC_UNUSED)
{
}

/**
 * multipath_object_manager_client_get_proxy_type:
 * @manager: A #GDBusObjectManagerClient.
 * @object_path: The object path of the remote object (unused).
 * @interface_name: (allow-none): Interface name of the remote object or %NULL to get the object proxy #GType.
 * @user_data: User data (unused).
 *
 * A #GDBusProxyTypeFunc that maps @interface_name to the generated #GDBusObjectProxy<!-- -->- and #GDBusProxy<!-- -->-derived types.
 *
 * Returns: A #GDBusProxy<!-- -->-derived #GType if @interface_name is not %NULL, otherwise the #GType for #multipathObjectProxy.
 */
GType
multipath_object_manager_client_get_proxy_type (GDBusObjectManagerClient *manager G_GNUC_UNUSED, const gchar *object_path G_GNUC_UNUSED, const gchar *interface_name, gpointer user_data G_GNUC_UNUSED)
{
  static gsize once_init_value = 0;
  static GHashTable *lookup_hash;
  GType ret;

  if (interface_name == NULL)
    return MULTIPATH_TYPE_OBJECT_PROXY;
  if (g_once_init_enter (&once_init_value))
    {
      lookup_hash = g_hash_table_new (g_str_hash, g_str_equal);
      g_hash_table_insert (lookup_hash, (gpointer) "com.redhat.multipath.Manager", GSIZE_TO_POINTER (MULTIPATH_TYPE_MANAGER_PROXY));
      g_hash_table_insert (lookup_hash, (gpointer) "com.redhat.multipath.map", GSIZE_TO_POINTER (MULTIPATH_TYPE_MAP_PROXY));
      g_hash_table_insert (lookup_hash, (gpointer) "com.redhat.multipath.map.path", GSIZE_TO_POINTER (MULTIPATH_TYPE_MAP_PATH_PROXY));
      g_once_init_leave (&once_init_value, 1);
    }
  ret = (GType) GPOINTER_TO_SIZE (g_hash_table_lookup (lookup_hash, interface_name));
  if (ret == (GType) 0)
    ret = G_TYPE_DBUS_PROXY;
  return ret;
}

/**
 * multipath_object_manager_client_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates #GDBusObjectManagerClient using multipath_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc. See g_dbus_object_manager_client_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call multipath_object_manager_client_new_finish() to get the result of the operation.
 *
 * See multipath_object_manager_client_new_sync() for the synchronous, blocking version of this constructor.
 */
void
multipath_object_manager_client_new (
    GDBusConnection        *connection,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GAsyncReadyCallback     callback,
    gpointer                user_data)
{
  g_async_initable_new_async (MULTIPATH_TYPE_OBJECT_MANAGER_CLIENT, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "flags", flags, "name", name, "connection", connection, "object-path", object_path, "get-proxy-type-func", multipath_object_manager_client_get_proxy_type, NULL);
}

/**
 * multipath_object_manager_client_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to multipath_object_manager_client_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with multipath_object_manager_client_new().
 *
 * Returns: (transfer full) (type multipathObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
multipath_object_manager_client_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}

/**
 * multipath_object_manager_client_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates #GDBusObjectManagerClient using multipath_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc. See g_dbus_object_manager_client_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See multipath_object_manager_client_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type multipathObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
multipath_object_manager_client_new_sync (
    GDBusConnection        *connection,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GError                **error)
{
  GInitable *ret;
  ret = g_initable_new (MULTIPATH_TYPE_OBJECT_MANAGER_CLIENT, cancellable, error, "flags", flags, "name", name, "connection", connection, "object-path", object_path, "get-proxy-type-func", multipath_object_manager_client_get_proxy_type, NULL);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}


/**
 * multipath_object_manager_client_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like multipath_object_manager_client_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call multipath_object_manager_client_new_for_bus_finish() to get the result of the operation.
 *
 * See multipath_object_manager_client_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
multipath_object_manager_client_new_for_bus (
    GBusType                bus_type,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GAsyncReadyCallback     callback,
    gpointer                user_data)
{
  g_async_initable_new_async (MULTIPATH_TYPE_OBJECT_MANAGER_CLIENT, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "flags", flags, "name", name, "bus-type", bus_type, "object-path", object_path, "get-proxy-type-func", multipath_object_manager_client_get_proxy_type, NULL);
}

/**
 * multipath_object_manager_client_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to multipath_object_manager_client_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with multipath_object_manager_client_new_for_bus().
 *
 * Returns: (transfer full) (type multipathObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
multipath_object_manager_client_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}

/**
 * multipath_object_manager_client_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like multipath_object_manager_client_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See multipath_object_manager_client_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type multipathObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
multipath_object_manager_client_new_for_bus_sync (
    GBusType                bus_type,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GError                **error)
{
  GInitable *ret;
  ret = g_initable_new (MULTIPATH_TYPE_OBJECT_MANAGER_CLIENT, cancellable, error, "flags", flags, "name", name, "bus-type", bus_type, "object-path", object_path, "get-proxy-type-func", multipath_object_manager_client_get_proxy_type, NULL);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}


